# Exemplo de .env para o shell Electron (apps/desktop)
# Copie para apps/desktop/.env para ajustar o launcher local.

# Porta onde a API interna será exposta
APP_PORT=3000

# URL que o BrowserWindow deve carregar após o health-check
APP_BASE_URL=http://localhost:3000

# ----- Licensing -----
# If set in development, the desktop process will pass this to the API and use it
# to call the cloud licensing endpoints. Example: https://licenses.example.com
LICENSE_API_BASE_URL=
LICENSE_SERVER_BASE_URL=

# Optional: override app version used during activation
APP_VERSION=
